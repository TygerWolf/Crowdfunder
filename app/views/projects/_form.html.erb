<% f.inputs do %>
  <%= f.input :name %>
  <%= f.input :description %>
  <h3>Tiers</h3>
  <div id='tiers'>
    <%= f.semantic_fields_for :tiers do |tier| %>
      <%= render 'project_fields', :f => task %>
    <div class='links'>
      <%= link_to_add_association 'add task', f, :tasks %>
    </div>
  <% end %>
  <% f.buttons do %>
    <%= f.submit 'Save' %>
  <% end %>
<% end %>